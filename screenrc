# use C-t as control key (so C-t c creates a new window etc)
escape "^T^T"
# do not trash BackSpace, usually DEL
bindkey -k kb
bindkey -d -k kb
# do not trash Delete, usually ESC [ 3 ~
bindkey -k kD
bindkey -d -k kD

